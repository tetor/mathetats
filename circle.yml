machine:
  node:
    version: 5.5.0
notify:
  webhooks:
    - url: https://webhooks.gitter.im/e/899c901c03a5a5337700
test:
  override:
    - MOCHA_FILE=$CIRCLE_TEST_REPORTS/mocha/tests.xml
    - mkdir -p $CIRCLE_TEST_REPORTS/mocha
    - `npm bin`/mocha --compilers js:babel-core/register --reporter mocha-junit-reporter
